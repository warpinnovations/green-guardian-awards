// Placeholder function to fetch bids assigned to the logged-in evaluator

import { Bid } from "@/app/types/bid";

/**
 * Fetch bids assigned to the logged-in evaluator.
 * Supabase logic will be added later.
 */
export async function getAssignedBids(): Promise<Bid[]> {
  return [];
}
